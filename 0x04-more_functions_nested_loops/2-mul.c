#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first variable
 * @b: second variable
 * Description: multiplies two integers
 * Return: the product of the two varable input
 */
int mul(int a, int b)
{
	int multiple;
		
	multiple = a * b;
	return (multiple);
}
