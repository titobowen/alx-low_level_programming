#include <stdio.h>
/*base 16 lowercase*/
int main(void)
{
	char a;
	char b = '0';

	for (b = '0'; b < '10'; b++);
	{
		putchar(b);
	}
	for (a = 'a'; a <= 'f'; a++);
	{
		putchar(a)
	}
	putchar('\n')
	return (0)
}
